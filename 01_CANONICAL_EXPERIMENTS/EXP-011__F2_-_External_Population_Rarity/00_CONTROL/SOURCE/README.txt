F2-A — WILDCHAT-4.8M EXTERNAL POPULATION RARITY

Purpose:
Apply the already frozen early-RSOS Surface-Free primary fingerprint and R1-Compatible secondary fingerprint to three official, previously sealed AllenAI WildChat-4.8M shards.

No WildChat training or threshold tuning is performed.
Raw conversation text is never written to result tables.
The runner verifies the local F1 final SHA-256 before the first reserved-data read.
The three official shard hashes are verified against AllenAI's published Git/Xet pointer hashes.

Primary output:
- external population AUC
- frozen later-lineage median/Q75/Q90/MAX tail exceedance rates
- exact binomial confidence bounds
- each later-lineage conversation's external percentile
- top anonymous external controls
- length/turn/code coarsened-exact nuisance-control diagnostic
- no-silent-omission ledger

Claim boundary:
F2-A estimates rarity in these three official shards. It does not by itself establish global uniqueness or literal neural-weight modification.
